import {Link} from 'react-router-dom';import PageHero from '../components/PageHero';import {useAppState} from '../hooks/useAppState';
export default function CounsellingCalendar(){const {counsellingEvents,catalogLoading}=useAppState();return <><PageHero title="Your Counselling Timeline" description="Sample JoSAA-style timeline. DEMO DATA — verify on official portal" crumb={<><Link to="/dashboard">Dashboard</Link> / Counselling Timeline</>}/><div className="container section">{catalogLoading&&<div className="source-note">Loading counselling events from backend…</div>}<div className="card" style={{marginBottom:16}}>🔔 <b>Choice Filling closes in 6 days</b> — based on the sample timeline below.</div>{counsellingEvents.map(e=><div className="card" style={{display:'flex',justifyContent:'space-between',alignItems:'center',marginBottom:10}} key={e.name}><div><b style={{fontSize:14}}>{e.name}</b><div style={{fontSize:12,color:'var(--ink-3)'}}>{e.date}</div></div><span className={`tag ${e.status==='upcoming'?'soon':''}`}>{e.status==='done'?'Completed':e.status==='now'?'Current':'Upcoming'}</span></div>)}</div></>}
