import {Link} from 'react-router-dom';import PageHero from '../components/PageHero';import Disclaimer from '../components/Disclaimer';import {useAppState} from '../hooks/useAppState';import {getDocuments} from '../services/counsellingService';
export default function Documents(){const {profile,selectedExamId,exams}=useAppState();const docs=getDocuments(profile,(exams.find(e=>e.id===selectedExamId)?.name || 'JEE Main'));return <><PageHero title="Admission Document Checklist" description="Based on your profile — requirements can vary, verify on the official counselling website." crumb={<><Link to="/dashboard">Dashboard</Link> / Document Checklist</>}/><div className="container section"><div className="card"><ul style={{paddingLeft:18}}>{docs.map(d=><li key={d} style={{fontSize:14,marginBottom:8,color:'var(--ink-2)'}}>{d}</li>)}</ul><Disclaimer>Requirements can vary by college and counselling authority. Verify the final list on the official counselling website.</Disclaimer></div></div></>}
